<UserControl x:Class="Diagnosis.Client.App.Controls.Search.QueryEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:search="clr-namespace:Diagnosis.Client.App.Controls.Search"
             xmlns:design="clr-namespace:Diagnosis.Client.App.DesignData"
             xmlns:con="http://schemas.kent.boogaart.com/converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:diag="http://schemas.smg.com/diagnosis"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance design:SampleQueryEditorViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding History.PrevOptionsCommand}">←</Button>
                <Button Command="{Binding History.NextOptionsCommand}">→</Button>
                <Separator Width="20"
                           Height="0" />
                <Button Command="{Binding Loader.OpenBufferCommand}"
                        Visibility="{Binding Loader.OpenBufferCommand.IsVisible, Converter={con:BooleanToVisibilityConverter}}">OpenBuffer</Button>
                <Button Command="{Binding Loader.LoadOptionsCommand}">Load</Button>
                <Button Command="{Binding Loader.SaveOptionsCommand}">Save</Button>
                <Separator Width="20" 
                           Height="0" />
                <Button Command="{Binding SendToSearchCommand}"
                        Visibility="{Binding SendToSearchCommand.IsVisible, Converter={con:BooleanToVisibilityConverter}}"
                        ToolTip="Искать по запросу">
                    <DockPanel>
                        <diag:AutoGreyableImage Source="{StaticResource findImage}" />
                    </DockPanel>
                </Button>
            </StackPanel>

            <TextBox Text="{Binding Loader.Buffer}"
                     Height="50"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     AcceptsReturn="True"
                     Visibility="{Binding Loader.ShowBuffer, Converter={con:BooleanToVisibilityConverter}}" />
        </StackPanel>

        <TreeView ItemsSource="{Binding QueryBlocks}"
                  Grid.Row="2">
            <ItemsControl.Resources>
                <DataTemplate x:Key="new">
                    <search:QueryBlock />
                </DataTemplate>
            </ItemsControl.Resources>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem"
                       BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="BorderThickness"
                            Value="0,1,0,0" />
                    <Setter Property="BorderBrush"
                            Value="{StaticResource AccentColorBrush2}" />
                    <Setter Property="Focusable"
                            Value="{Binding AnyPopupOpen, Converter={StaticResource NegateBoolean}}" />
                </Style>
            </TreeView.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <search:QueryBlock />
                </HierarchicalDataTemplate>
            </ItemsControl.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>