<Window x:Class="Diagnosis.Client.App.Windows.AdminSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:diag="http://schemas.smg.com/diagnosis"
        xmlns:controls="clr-namespace:Diagnosis.Client.App.Controls"
        diag:DialogCloser.DialogResult="{Binding DialogResult}"
        Title="Настройки"
        SizeToContent="Height"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="300"
        WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True">
    <Window.Resources>
    </Window.Resources>
    <Grid Margin="{StaticResource SreenPadding}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <controls:NewPasswordControl Grid.IsSharedSizeScope="True"/>

        <StackPanel Grid.Row="1"
                    Style="{StaticResource DialogButtonsPanel}">
            <Button Name="saveButton"
                    IsDefault="True"
                    Command="{Binding OkCommand}">Сохранить</Button>
            <Button Name="cancelButton"
                    IsCancel="True"
                    Command="{Binding CancelCommand}">Отмена</Button>
        </StackPanel>
    </Grid>
</Window>