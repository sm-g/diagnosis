<Window x:Class="Diagnosis.App.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:windows="clr-namespace:Diagnosis.App.Windows"        
        windows:DialogCloser.DialogResult="{Binding DialogResult}"
        Title="Настройки" Height="150" Width="300" ShowInTaskbar="False" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.Resources>
    </Window.Resources>
    <Grid Margin="0,0,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ListBox Margin="10,10,0,0">
            <CheckBox IsChecked="{Binding ShowIcdDisease}" Content="Использовать _МКБ"/>
            <CheckBox IsChecked="{Binding OnlyTopLevelIcdDisease}" Content="_Болезни МКБ только верхнего уровня"/>
        </ListBox>

        <StackPanel Grid.Row="1" Style="{StaticResource DialogButtonsPanel}">
            <Button Name="saveButton" IsDefault="True" Command="{Binding SaveCommand}">_Сохранить</Button>
            <Button Name="cancelButton" IsCancel="True">Отмена</Button>
        </StackPanel>
    </Grid>
</Window>
