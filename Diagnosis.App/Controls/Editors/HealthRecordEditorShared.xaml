<UserControl xmlns:editors="clr-namespace:Diagnosis.App.Controls.Editors"
             xmlns:controls="clr-namespace:Diagnosis.App.Controls"
             x:Class="Diagnosis.App.Controls.Editors.HealthRecordEditorShared"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Diagnosis.App.ViewModels"             
             x:Name="editor"
             Padding="0,10,0,0"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="600">
    <!--DataContext is HrEditorViewModel-->
    <UserControl.Resources>
        <Style TargetType="Label" BasedOn="{StaticResource label90}" />
    </UserControl.Resources>

    <!--DataContext is editing HealthRecord-->
    <Grid x:Name="hr_grid" DataContext="{Binding HealthRecord}" MouseDown="hr_grid_MouseDown" Focusable="True" Background="Transparent"> 
        <Grid.InputBindings>
            <KeyBinding Modifiers="Control" Key="S" Command="{Binding Editable.CommitCommand}" />
            <KeyBinding Key="Escape" Command="{Binding Editable.RevertCommand}" />
            <KeyBinding Key="Delete" Command="{Binding Editable.DeleteCommand}" />
        </Grid.InputBindings>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <StackPanel VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Padding" Value="5,10" />
                    <Setter Property="FontSize" Value="16 " />
                    <Setter Property="Margin" Value="0,5,5,5" />
                </Style>
            </StackPanel.Resources>

            <Button Command="{Binding DataContext.MoveHrSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl, AncestorLevel=2}}"
                    CommandParameter="{StaticResource True}" ToolTip="Открыть предыдущую запись (Crtl + Стрелка вверх)">↑</Button>
            <Button Command="{Binding DataContext.MoveHrSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl, AncestorLevel=2}}"
                    CommandParameter="{StaticResource False}" ToolTip="Открыть следующую запись (Crtl + Стрелка вниз)" >↓</Button>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <StackPanel.Resources>
                <Style TargetType="DockPanel" BasedOn="{StaticResource formLine}" />
            </StackPanel.Resources>
            <DockPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <controls:AutoComplete DataContext="{Binding DataContext.AutoComplete, ElementName=editor}" />
                    <TextBox Text="{Binding NumValue, StringFormat={}{0:G6}, TargetNullValue=''}"
                             ToolTip="Числовое значение"
                             Width="60"
                             Margin="10,0,0,0"
                             Grid.Column="1"
                             HorizontalContentAlignment="Right" HorizontalAlignment="Right">
                        <controls:WatermarkService.Watermark>
                            <TextBlock Style="{StaticResource watermark}" Margin="5,0,0,0">число</TextBlock>
                        </controls:WatermarkService.Watermark>
                    </TextBox>
                </Grid>
            </DockPanel>
            <DockPanel>
                <editors:CategoryChooser HorizontalAlignment="Left" />
            </DockPanel>
            <DockPanel>
                <Label Content="дата" />
                <editors:ComboBoxDatePicker Year="{Binding FromYear, Mode=TwoWay}"
                                        Month="{Binding FromMonth, Mode=TwoWay}"
                                        Day="{Binding FromDay, Mode=TwoWay}" />
                <Label Content="давность" Margin="10,0,0,0" Width="60" />
                <editors:DateOffsetPicker DateOffset="{Binding DateOffset}">
                    <editors:DateOffsetPicker.Resources>
                        <Style TargetType="DockPanel" />
                    </editors:DateOffsetPicker.Resources>
                </editors:DateOffsetPicker>
            </DockPanel>
            <DockPanel DataContext="{Binding DataContext, ElementName=editor}" Visibility="{Binding ShowIcdDisease, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Label Content="диагноз МКБ" />
                <StackPanel>
                    <controls:SearchTree DataContext="{Binding DiagnosisSearch}" />
                </StackPanel>
            </DockPanel>
            <DockPanel>
                <Label Content="комментарий" />
                <TextBox Text="{Binding Comment}" />
            </DockPanel>
        </StackPanel>

        <StackPanel Grid.Column="2" Margin="20,0,0,0" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0,0,0,10" />
                    <!--<Setter Property="Padding" Value="3" />-->
                    <!--<Setter Property="Height" Value="23" />-->
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                </Style>
                <Style TargetType="Image">
                    <Setter Property="Margin" Value="0,0,5,0" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                    <!--<Setter Property="Padding" Value="3" />-->
                    <!--<Setter Property="Height" Value="23" />-->
                </Style>
            </StackPanel.Resources>

            <Button Command="{Binding DataContext.AddHealthRecordCommand, RelativeSource={RelativeSource AncestorType=UserControl, AncestorLevel=2}}" 
                    ToolTip="Добавить запись (Ctrl + N)"
                    Margin="0,0,0,20">
                <DockPanel>
                    <Image Source="{StaticResource addImage}" />
                    <TextBlock Text="Добавить" />
                </DockPanel>
            </Button>
            <Button Command="{Binding Editable.CommitCommand}" ToolTip="Закрыть редактор, сохранив все изменения (Ctrl + S)">
                <DockPanel>
                    <Image Source="{StaticResource saveImage}" />
                    <TextBlock Text="Закрыть" />
                </DockPanel>
            </Button>
            <Button Command="{Binding Editable.RevertCommand}" ToolTip="Закрыть редактор без сохранения изменений (Esc)">
                <DockPanel>
                    <Image Source="{StaticResource undoImage}" />
                    <TextBlock Text="Отменить" />
                </DockPanel>
            </Button>
            <Button Command="{Binding Editable.DeleteCommand}" Margin="0,10,0,0" ToolTip="Удалить текущую запись (Del)">
                <DockPanel>
                    <Image Source="{StaticResource deleteImage}"  Width="16" />
                    <TextBlock Text="Удалить" />
                </DockPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>