<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Converters="clr-namespace:Diagnosis.App.Converters" 
             x:Class="Diagnosis.App.Controls.Editors.SexChooser"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <RadioButton IsChecked="{Binding IsMale, Converter={StaticResource NullableBoolean}, ConverterParameter='true'}"
                     Content="муж" />
        <RadioButton Grid.Column="1" IsChecked="{Binding IsMale, Converter={StaticResource NullableBoolean}, ConverterParameter='false'}"
                     Content="жен" />
    </Grid>
</UserControl>