<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Class="Diagnosis.App.Controls.Editors.SexChooser"
             mc:Ignorable="d"
             MinWidth="100"
             d:DesignHeight="30"
             d:DesignWidth="100">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <RadioButton IsChecked="{Binding IsMale}"
                     Content="муж" />
        <RadioButton Grid.Column="1"
                     IsChecked="{Binding IsFemale}"
                     Content="жен" />
        <RadioButton Grid.Column="2"
                     Visibility="{Binding WithUnknown, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     IsChecked="{Binding UnknownSex}"
                     Content="?" />
    </Grid>
</UserControl>